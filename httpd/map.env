# Palworld REST API base URL as reachable from the live-map process or container.
# Compose maps host.docker.internal to the Docker host on Linux.
PALWORLD_REST_URL=http://pal:8212

# Palworld REST API admin password. This stays in the backend and is never sent to browsers.
PALWORLD_ADMIN_PASSWORD=admin

# Run with fictional data and without a Palworld server. When true, the two settings above are optional.
DEMO_MODE=false

# Host port published by Compose for the live-map website.
HTTP_PORT=8080

# Address the Go HTTP server listens on. Keep :8080 when using Compose.
ADDR=:8080

# Optional path prefix for serving the site behind a reverse proxy at a subpath
# (e.g. https://example.com/palworld-map/). The reverse proxy must forward the
# prefix as-is (no stripping). Leave empty to serve at the root path.
BASE_PATH=/palworld-map

# How often the online-player list is refreshed. Minimum: 2s.
POLL_INTERVAL=5s

# Timeout for player and server-information requests. Must be shorter than POLL_INTERVAL.
UPSTREAM_TIMEOUT=4s

# Whether to poll the optional game-data endpoint for bases, Pals, and NPCs.
WORLD_DATA_ENABLED=true

# How often world objects are refreshed when WORLD_DATA_ENABLED is true. Minimum: 5s.
WORLD_POLL_INTERVAL=15s

# Timeout for the larger world-object snapshot. Must be shorter than WORLD_POLL_INTERVAL.
WORLD_TIMEOUT=10s

# Enrich REST-visible players with progress from immutable native save backups.
SAVE_DATA_ENABLED=true

# Read-only SaveGames/0 directory containing one or more world-ID directories.
PALWORLD_SAVE_ROOT=/palworld-data/Pal/Saved/SaveGames/0

# Optional exact 32-character world ID. Required when the save root contains
# more than one usable world.
PALWORLD_SAVE_WORLD_ID=

# Save enrichment timing. The timeout covers all player files in one generation.
SAVE_POLL_INTERVAL=30s
SAVE_TIMEOUT=20s

# Optional self-only saved-character connection. This requires save integration.
PLAYER_CLAIMS_ENABLED=false
